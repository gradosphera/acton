---
title: "Forking from Network"
description: "Run local node with testnet/mainnet state, including historical block forks"
---

Fork mode lets the local node resolve account state from a remote network while keeping execution local.

## Basic Forking

```bash
# Fork from testnet
acton litenode start --fork-net testnet

# Fork from mainnet
acton litenode start --fork-net mainnet
```

## Historical Fork at a Specific Block

```bash
acton litenode start --fork-net testnet --fork-block-number 55000000
```

This is useful for deterministic debugging of historical behavior.

## API Key

Use `--api-key` for TonCenter access:

```bash
acton litenode start --fork-net mainnet --api-key YOUR_API_KEY
```

You can also set `TONCENTER_API_KEY` and omit `--api-key`.

## Configure Defaults in `Acton.toml`

```toml title="Acton.toml"
[litenode]
fork-net = "testnet"
fork-block-number = 55000000
```

Now `acton litenode start` automatically uses these defaults.
