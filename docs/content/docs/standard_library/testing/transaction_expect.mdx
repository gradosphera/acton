---
title: "transaction_expect"
description: "transaction_expect.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

<span id="Expectation<SendResultList>.toHaveSuccessfulDeploy"></span>
## `Expectation<SendResultList>.toHaveSuccessfulDeploy`

```tolk
fun Expectation<SendResultList>.toHaveSuccessfulDeploy<Msg = never>(self, params: SearchParams = {})
```

Expect the transaction list to have a successful deploy transaction.

Transaction is considered as "deploy" if previous status was "NonExisting" and end status was "Active".

Example:

```
expect(txs).toHaveSuccessfulDeploy({
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L19" />

<span id="Expectation<SendResultList>.toHaveSuccessfulTx"></span>
## `Expectation<SendResultList>.toHaveSuccessfulTx`

```tolk
fun Expectation<SendResultList>.toHaveSuccessfulTx<Msg = never>(self, params: SearchParams = {})
```

Expect the transaction list to have a successful transaction.

Transaction is considered as "successful" if exit code is 0, but exit code
can be overridden by the `exit_code` parameter.

Example:
```
expect(txs).toHaveSuccessfulTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L48" />

<span id="Expectation<SendResultList>.toHaveTx"></span>
## `Expectation<SendResultList>.toHaveTx`

```tolk
fun Expectation<SendResultList>.toHaveTx<Msg = never>(self, params: SearchParams = {})
```

Expect the transaction list to have a transaction with specified parameters.

Example:
```
expect(txs).toHaveTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L75" />

<span id="Expectation<SendResultList>.toNotHaveTx"></span>
## `Expectation<SendResultList>.toNotHaveTx`

```tolk
fun Expectation<SendResultList>.toNotHaveTx<Msg = never>(self, params: SearchParams = {})
```

Expect the transaction list to NOT have a transaction with specified parameters.

Example:
```
expect(txs).toNotHaveTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L99" />

<span id="Expectation<SendResultList>.toHaveBouncedTx"></span>
## `Expectation<SendResultList>.toHaveBouncedTx`

```tolk
fun Expectation<SendResultList>.toHaveBouncedTx<Msg = never>(self, params: SearchParams = {})
```

Expect the transaction list to have a **bounced** transaction.

Example:
```
expect(txs).toHaveBouncedTx({
    from: deployer.address,
    to: counter.address,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L123" />

<span id="Expectation<SendResultList>.toHaveFailedTx"></span>
## `Expectation<SendResultList>.toHaveFailedTx`

```tolk
fun Expectation<SendResultList>.toHaveFailedTx<Msg = never>(self, params: SearchParams = {})
```

Expect the transaction list to have a **failed** transaction.
Exit code in parameters must be set to non zero value.

Example:
```
expect(txs).toHaveFailedTx({
    from: deployer.address,
    to: counter.address,
    exit_code: 123,
});
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L150" />

<span id="Expectation<SendResultList>.toEmitExternalMessage"></span>
## `Expectation<SendResultList>.toEmitExternalMessage`

```tolk
fun Expectation<SendResultList>.toEmitExternalMessage<Msg>(self)
```

Expect the transaction to emit an external message of given type.

Example:
```
expect(txs).toEmitExternalMessage<IncreaseCounter>();
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L178" />

<span id="Expectation<SendResult>.toConsumeLessThan"></span>
## `Expectation<SendResult>.toConsumeLessThan`

```tolk
fun Expectation<SendResult>.toConsumeLessThan(self, amount: int)
```

Expect the transaction to consume less than the expected amount of gas.

Example:
```
val result = net.send(deployer.address, message, SEND_MODE_PAY_FEES_SEPARATELY);
expect(result.at(0)).toConsumeLessThan(1000);
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/transaction_expect.tolk#L214" />

