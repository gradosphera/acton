---
title: "outlist_expect"
description: "outlist_expect.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

<span id="Expectation<OutActionList>.toBeNonEmpty"></span>
## `Expectation<OutActionList>.toBeNonEmpty`

```tolk
fun Expectation<OutActionList>.toBeNonEmpty(self)
```

Expect the out action list to be non empty.

Example:
```
val out_actions = vm.outActions();
expect(out_actions).toBeNonEmpty();
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/outlist_expect.tolk#L15" />

<span id="Expectation<OutActionList>.toBeEmpty"></span>
## `Expectation<OutActionList>.toBeEmpty`

```tolk
fun Expectation<OutActionList>.toBeEmpty(self)
```

Expect the out action list to be empty.

Example:
```
val out_actions = vm.outActions();
expect(out_actions).toBeEmpty();
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/outlist_expect.tolk#L26" />

<span id="Expectation<OutActionList>.toBeSendMessageAt"></span>
## `Expectation<OutActionList>.toBeSendMessageAt`

```tolk
fun Expectation<OutActionList>.toBeSendMessageAt<Msg>(self, index: int)
```

Expect the out action at the given index to be a send message.

Example:
```
val out_actions = vm.outActions();
expect(out_actions).toBeSendMessageAt<IncreaseCounter>(0);
```

Note that you should provide the message type as a generic parameter.

If the action is not a send message, the test will fail.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/testing/outlist_expect.tolk#L41" />

