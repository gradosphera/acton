---
title: "message"
description: "message.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

<span id="MessageRelaxedGeneric"></span>
## `MessageRelaxedGeneric`

```tolk
struct MessageRelaxedGeneric {
    info: IntMsgInfoRelaxed
    init: Maybe<Either<StateInit, Cell<StateInit>>>
    body: RemainingBitsAndRefs
}
```

Represents a generic relaxed message. Unlike [MessageRelaxed](#MessageRelaxed), this message does not
load the body of the message. Instead body is represented as a slice and can be parsed
later manually (for example to load the opcode).

Example:
```tolk
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val message = action.loadGenericMessage();
    val opcode = message.loadOpcode();
    println(opcode);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L18" />

<span id="MessageRelaxedGeneric.loadOpcode"></span>
## `MessageRelaxedGeneric.loadOpcode`

```tolk
fun MessageRelaxedGeneric.loadOpcode(self, skip_bounce: bool = false): int?
```

Loads the opcode of the message.

If skip_bounce is true, the 0xFFFFFFFF prefix is skipped.

Example:
```tolk
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val message = action.loadGenericMessage();
    val opcode = message.loadOpcode(true);
    println(opcode);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L38" />

<span id="ExtMessageRelaxedGeneric"></span>
## `ExtMessageRelaxedGeneric`

```tolk
struct ExtMessageRelaxedGeneric {
    info: ExtMsgInfoRelaxed
    init: Maybe<Either<StateInit, Cell<StateInit>>>
    body: RemainingBitsAndRefs
}
```

Represents a generic external message. Unlike [ExtMessageRelaxed](#ExtMessageRelaxed), this message does not
load the body of the message. Instead body is represented as a slice and can be parsed
later manually (for example to load the opcode).

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L45" />

<span id="ExtMessageRelaxedGeneric.loadOpcode"></span>
## `ExtMessageRelaxedGeneric.loadOpcode`

```tolk
fun ExtMessageRelaxedGeneric.loadOpcode(self, skip_bounce: bool = false): int?
```

Loads the opcode of the message.

If skip_bounce is true, the 0xFFFFFFFF prefix is skipped.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L54" />

<span id="MessageRelaxed"></span>
## `MessageRelaxed`

```tolk
@overflow1023_policy("suppress")
struct MessageRelaxed<X> {
    info: IntMsgInfoRelaxed
    init: Maybe<Either<StateInit, Cell<StateInit>>>
    body: Either<X, Cell<X>>
}
```

Represents a generic message. Unlike [MessageRelaxedGeneric](#MessageRelaxedGeneric), this message loads
the body of the message with passed type.

If actual message body has a different type, loading will fail with exit code 63.

Example:
```tolk
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val message = action.loadMessage<IncreaseCounter>();
    val body = message.loadBody();
    println(body.increaseBy);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L73" />

<span id="MessageRelaxed<X>.loadBody"></span>
## `MessageRelaxed<X>.loadBody`

```tolk
fun MessageRelaxed<X>.loadBody(self): X
```

Loads the body of the message.

Note that if message type mismatch, loading will fail with exit code 63.

Example:
```tolk
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val message = action.loadMessage<IncreaseCounter>();
    val body = message.loadBody();
    println(body.increaseBy);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L94" />

<span id="ExtMessageRelaxed"></span>
## `ExtMessageRelaxed`

```tolk
@overflow1023_policy("suppress")
struct ExtMessageRelaxed<X> {
    info: ExtMsgInfoRelaxed
    init: Maybe<Either<StateInit, Cell<StateInit>>>
    body: Either<X, Cell<X>>
}
```

Represents a generic external message. Unlike [ExtMessageRelaxedGeneric](#ExtMessageRelaxedGeneric), this message
loads the body of the message with passed type.

If actual message body has a different type, loading will fail with exit code 63.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L105" />

<span id="ExtMessageRelaxed<X>.loadBody"></span>
## `ExtMessageRelaxed<X>.loadBody`

```tolk
fun ExtMessageRelaxed<X>.loadBody(self): X
```

Loads the body of the message.

Note that if message type mismatch, loading will fail with exit code 63.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L115" />

<span id="IntMsgInfoRelaxed"></span>
## `IntMsgInfoRelaxed`

```tolk
struct (0b0) IntMsgInfoRelaxed {
    ihr_disabled: bool
    bounce: bool
    bounced: bool
    src: any_address
    dest: address
    value: CurrencyCollection
    extra_flags: varuint16
    fwd_fee: coins
    created_lt: uint64
    created_at: uint32
}
```

Represents common message info for internal relaxed message.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L123" />

<span id="ExtMsgInfoRelaxed"></span>
## `ExtMsgInfoRelaxed`

```tolk
struct (0b11) ExtMsgInfoRelaxed {
    src: address
    dest: any_address
    created_lt: uint64
    created_at: uint32
}
```

Represents common message info for external relaxed message.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L137" />

<span id="CurrencyCollection"></span>
## `CurrencyCollection`

```tolk
struct CurrencyCollection {
    /// Amount in nanoton.
    grams: coins
    /// Extra currencies.
    other: map<int32, varuint32>
}
```

Represents a currency collection as TON amount + extra currencies.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L145" />

<span id="InternalMessage"></span>
## `InternalMessage`

```tolk
struct (0b0) InternalMessage {
    ihrDisabled: bool
    bounce: bool
    bounced: bool
    src: address
    dest: address
    value: CurrencyCollection
    extraFlags: varuint16
    forwardFee: coins
    createdLT: uint64
    createdAt: uint32
}
```

```tlb
int_msg_info$0 ihr_disabled:Bool bounce:Bool bounced:Bool
               src:MsgAddressInt dest:MsgAddressInt
               value:CurrencyCollection extra_flags:(VarUInteger 16) fwd_fee:Grams
               created_lt:uint64 created_at:uint32 = CommonMsgInfo;
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L158" />

<span id="ExternalInMessageInfo"></span>
## `ExternalInMessageInfo`

```tolk
struct (0b10) ExternalInMessageInfo {
    src: any_address
    dest: address
    importFee: coins
}
```

```tlb
ext_in_msg_info$10 src:MsgAddressExt dest:MsgAddressInt
                   import_fee:Grams = CommonMsgInfo;
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L175" />

<span id="ExternalOutMessageInfo"></span>
## `ExternalOutMessageInfo`

```tolk
struct (0b11) ExternalOutMessageInfo {
    src: address
    dest: any_address
    createdLt: uint64
    createdAt: uint32
}
```

```tlb
ext_out_msg_info$11 src:MsgAddressInt dest:MsgAddressExt
                    created_lt:uint64 created_at:uint32 = CommonMsgInfo;
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L185" />

<span id="Message"></span>
## `Message`

```tolk
struct Message<TBody = RemainingBitsAndRefs, TInfo = CommonMessageInfo> {
    info: TInfo
    init: Maybe<Either<StateInit, Cell<StateInit>>>
    body: Either<TBody, Cell<TBody>>
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L197" />

<span id="Message<TBody, TInfo>.loadBody"></span>
## `Message<TBody, TInfo>.loadBody`

```tolk
fun Message<TBody, TInfo>.loadBody(self): TBody
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/message.tolk#L203" />

