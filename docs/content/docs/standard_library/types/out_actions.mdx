---
title: "out_actions"
description: "out_actions.tolk standard library file"
---

import { SourceCodeLink } from '@/components/SourceCodeLink';

<span id="OutList"></span>
## `OutList`

```tolk
struct OutList {
    action: OutAction
}
```

Represents a node in the out list.
This is an internal type, you shouldn't use it directly.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L8" />

<span id="OutActionSendMessage"></span>
## `OutActionSendMessage`

```tolk
struct (0x0ec3c86d) OutActionSendMessage {
    prev: Cell<OutList>
    /// Mode of the send message. See `SEND_MODE_*` constants.
    mode: uint8
    /// Message to send.
    /// 
    /// To load the actual message, use [OutActionSendMessage.loadMessage] method.
    out_msg: cell
}
```

Represents send message action.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L13" />

<span id="OutActionSendMessage.loadMessage"></span>
## `OutActionSendMessage.loadMessage`

```tolk
fun OutActionSendMessage.loadMessage<Msg>(self): MessageRelaxed<Msg>
```

Loads the message from the out message cell.

Note that you should provide the message type as a generic parameter.

If you want to access the body of the message, use [OutActionSendMessage.loadBody](#OutActionSendMessage.loadBody) method.

Example:
```
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val message = action.loadMessage<IncreaseCounter>();
    val body = message.loadBody();
    println(body.increaseBy);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L39" />

<span id="OutActionSendMessage.loadBody"></span>
## `OutActionSendMessage.loadBody`

```tolk
fun OutActionSendMessage.loadBody<Msg>(self): Msg
```

Loads the body of the message.

Note that you should provide the message type as a generic parameter.

Example:
```
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val body = action.loadBody<IncreaseCounter>();
    println(body.increaseBy);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L56" />

<span id="OutActionSendMessage.loadGenericMessage"></span>
## `OutActionSendMessage.loadGenericMessage`

```tolk
fun OutActionSendMessage.loadGenericMessage(self): MessageRelaxedGeneric
```

Loads the generic message from the out message cell.
Unlike [OutActionSendMessage.loadMessage](#OutActionSendMessage.loadMessage), this method does not require type of the message to load.
Instead it loads the message with body as a slice. Use this method if don't know the type of the message.

Example:
```
val out_actions = vm.outActions();
val action = out_actions.at(0);
if (action is OutActionSendMessage) {
    val message = action.loadGenericMessage();
    val opcode = message.loadOpcode();
    println(opcode);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L74" />

<span id="OutActionSetCode"></span>
## `OutActionSetCode`

```tolk
struct (0xad4de08e) OutActionSetCode {
    prev: Cell<OutList>
    /// New code to set.
    new_code: cell
}
```

Represents set code action.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L79" />

<span id="OutActionReserveCurrency"></span>
## `OutActionReserveCurrency`

```tolk
struct (0x36e6b809) OutActionReserveCurrency {
    prev: Cell<OutList>
    /// Mode of the reserve currency. See `RESERVE_MODE_*` constants.
    mode: uint8
    /// Currency to reserve.
    currency: CurrencyCollection
}
```

Represents reserve currency action.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L86" />

<span id="OutActionChangeLibrary"></span>
## `OutActionChangeLibrary`

```tolk
struct (0x26fa1dd4) OutActionChangeLibrary {
    prev: Cell<OutList>
    /// Mode of the change library.
    mode: uint7
    /// Library reference, either hash or cell reference.
    libref: LibRef
}
```

Represents change library action.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L95" />

<span id="LibRefHash"></span>
## `LibRefHash`

```tolk
struct (0b0) LibRefHash {
    /// Hash of the library.
    lib_hash: bits256
}
```

Represents library reference as a hash.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L104" />

<span id="LibRefRef"></span>
## `LibRefRef`

```tolk
struct (0b1) LibRefRef {
    /// Cell reference to the library.
    library: cell
}
```

Represents library reference as a cell reference.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L110" />

<span id="OutAction.kind"></span>
## `OutAction.kind`

```tolk
fun OutAction.kind(self): slice
```

Returns the kind of the out action as a string.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L126" />

<span id="OutActionList.at"></span>
## `OutActionList.at`

```tolk
fun OutActionList.at(self, i: int): OutAction
```

Returns the out action at the given index.

NOTE: this list is reversed, so the last action is at index 0.

Example:
```
val out_actions = vm.outActions();
val action = out_actions.at(0);
println(action.kind());
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L158" />

<span id="OutActionList.getSendMessageAt"></span>
## `OutActionList.getSendMessageAt`

```tolk
fun OutActionList.getSendMessageAt(self, i: int): OutActionSendMessage?
```

Returns the send message action at the given index.

If the action is not a send message, returns null.

Example:
```
val out_actions = vm.outActions();
val action = out_actions.getSendMessageAt(0);
if (action != null) {
    println(action.mode);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L175" />

<span id="OutActionList.getSendMessageBodyAt"></span>
## `OutActionList.getSendMessageBodyAt`

```tolk
fun OutActionList.getSendMessageBodyAt<Msg>(self, i: int): Msg?
```

Returns the body of the send message at the given index.

Note that you should provide the message type as a generic parameter.

If the action is not a send message, returns null.

Example:
```
val out_actions = vm.outActions();
val body = out_actions.getSendMessageBodyAt<IncreaseCounter>(0);
if (body != null) {
    println(body.increaseBy);
}
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L197" />

<span id="OutMessage.outActions"></span>
## `OutMessage.outActions`

```tolk
fun OutMessage.outActions(self, mode: int): OutActionList
```

Returns the out actions for the message.

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L216" />

<span id="vm.outActions"></span>
## `vm.outActions`

```tolk
fun vm.outActions(): OutActionList
```

Returns the out actions from the control register C5 at that moment.

Example:
```
val out_actions = vm.outActions();
println(out_actions.size());
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L231" />

<span id="vm.parseOutActions"></span>
## `vm.parseOutActions`

```tolk
fun vm.parseOutActions(cell: cell): OutActionList
```

Parses the out actions from the raw cell (like C5 register).

Example:
```
val out_actions = vm.parseOutActions(vm.getC5());
println(out_actions.size());
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L242" />

<span id="LibRef.fromTuple"></span>
## `LibRef.fromTuple`

```tolk
fun LibRef.fromTuple(data: tuple): LibRef
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L290" />

<span id="OutAction.fromTuple"></span>
## `OutAction.fromTuple`

```tolk
fun OutAction.fromTuple(data: tuple): OutAction
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L300" />

<span id="OutActionChangeLibrary.toTuple"></span>
## `OutActionChangeLibrary.toTuple`

```tolk
fun OutActionChangeLibrary.toTuple(self)
```

<SourceCodeLink href="https://github.com/i582/acton/blob/master/lib/types/out_actions.tolk#L316" />

