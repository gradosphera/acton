;; Auto-generated by tasm::func_decompile
;; Pipeline: stack-lift -> structured-flow -> pattern-rewrite -> fallback
#include "stdlib.fc";
#pragma version >=0.4.0;

;; recovered_methods: 2
;; call_dict_targets: 1

int __dict_method_1(int arg0, int arg1) impure method_id(1);

() recv_internal(int msg_value, cell in_msg_full, slice in_msg_body) impure {
    ;; dict_method_id: 0
    ;; recovered role: recv_internal handler
    ;; inferred params: msg_value, in_msg_full, in_msg_body
    ;; calls helpers: CALLDICT 1
    var v0 = __dict_method_1(msg_value, 5);
    ;; low-level fallback (TASM)
    ;; DROP
    ;; DROP
    ;; PUSHINT_4 5
    ;; CALLDICT 1
    ;; DROP
}

int __dict_method_1(int arg0, int arg1) impure method_id(1) {
    ;; dict_method_id: 1
    ;; recovered role: helper (called via CALLDICT)
    ;; inferred params: arg0, arg1
    var v0 = (arg0) * (7);
    var v1 = (v0) + (arg1);
    return v1;
    ;; low-level fallback (TASM)
    ;; SWAP
    ;; MULINT 7
    ;; SWAP
    ;; ADD
}

