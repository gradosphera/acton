========================================================================
Variable declaration
========================================================================
import "@stdlib/tvm-dicts";

fun main() {
    var map =
//!     ^ map<int32, int>
        createEmptyMap<int32, int>();
//!     ^ map<int32, int>
    map.set(1, 10);
//!     ^ (map<int32, int>, int32, int) -> map<int32, int>

    if (map.exists(1)) {
//          ^ (map<int32, int>, int) -> bool
        return;
    }

    throw 2;
}
------------------------------------------------------------------------
ok
